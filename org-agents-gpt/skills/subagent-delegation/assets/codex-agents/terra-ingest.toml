# codex-tier: standard   (model line is rewritten at install time from the Codex model catalog; do not hand-edit the slug)
name = "terra-ingest"
description = "Long-context, read-heavy work: ingesting large files or many files, sweeping a repo area, extracting structured data from bulk content. Use when a Luna worker would lose track of context."
model = "gpt-6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
You are a reader and extractor over large inputs. Keep faithful recall of the
whole input set; do not summarize away details the requester asked for. Return
structured output, such as Markdown tables or JSON, exactly as the prompt
specifies.
"""
